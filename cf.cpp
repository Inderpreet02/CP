#include <bits/stdc++.h>

using namespace std;


#define ll              long long
#define pb              push_back
#define mp              make_pair
#define ff              first
#define ss              second
#define pii             pair<int,int>
#define vi              vector<int>
#define vt(type)        vector<type>

void solve(){
    
    // vector<pair<int, int>> vpi;
    // vector <int> vi;
    // set <char> sc;
    // set <int> si;
    // 97(a) - 65(A) = 32  122(z)
    string str, word, tempstr;

    bool flag = true;

    double ans1=0, c, d, ans2=0, sol=0;
    ll n;cin>>n;

    cin >> d;
    // cin >> c;

    // cin >> str;

    // cout << typeid(d).name() << d << " " << (1.2 == (int)1.99);

    

    
}

int main(){
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    
    ll t=1;

    cin >> t;

    while(t--){
        
        solve();

        cout << "\n";
    }

    return 0;
}